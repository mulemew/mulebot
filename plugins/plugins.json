{
  "_comment": [
    "Optional. Delete this file and everything still works with defaults.",
    "'disabled' lists plugin names (the filename without .js) not to load.",
    "'config' is handed to each plugin as plugin.config.",
    "'urls' lists https URLs fetched at every boot.",
    "",
    "The same three are settable from the environment, which is the only way",
    "on a host where this file cannot be edited:",
    "  PLUGINS_IGNORED=a,b     PLUGINS_URLS=https://...",
    "",
    "Nothing ships disabled. healthz is bundled but binds nothing unless the",
    "platform injects PORT, so it costs nothing to leave loaded."
  ],

  "disabled": [],

  "config": {},

  "urls": []
}
